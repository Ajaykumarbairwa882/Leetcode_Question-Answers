class Solution {
public:
    bool isPowerOfFour(int n) {
        // base cases
        if (n == 1) return true;
        if (n <= 0 || n % 4 != 0) return false;

        // recursive call
        return isPowerOfFour(n / 4);
    }
};
